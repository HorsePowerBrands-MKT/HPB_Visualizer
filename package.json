{
  "name": "hpb-visualizer",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter gatsby-glass dev",
    "dev:gatsby": "pnpm --filter gatsby-glass dev",
    "build": "pnpm -r build",
    "build:gatsby": "pnpm --filter gatsby-glass build",
    "lint": "pnpm -r lint",
    "clean": "pnpm -r clean"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "@types/react": "^18.2.48",
    "@types/react-dom": "^18.2.18",
    "typescript": "^5.3.3"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["sharp", "protobufjs", "unrs-resolver"]
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
